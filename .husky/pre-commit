#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

printf '\n[pre-commit] validating taxonomy...\n'
npm run validate:taxonomy || exit 1
printf '\n[pre-commit] typecheck...\n'
npm run typecheck || exit 1
printf '\n[pre-commit] lint...\n'
npm run lint --max-warnings=999 || exit 1
